# PTG Oil System — Full Hardening Validation Report

**تاریخ:** 2026-08-29
**دامنه:** فاز نهایی سخت‌سازی — موارد P1/P2/P3/P4 و نگرانی‌های باقی‌ماندهٔ فاز P0.
**پیش‌نیازها:** `PTG_12_MONTH_PRODUCTION_SIMULATION_REPORT.md` و `PTG_P0_FIX_VALIDATION_REPORT.md`
(هر دو کامل خوانده شدند؛ هیچ کار انجام‌شده‌ای تکرار نشد).

---

## ۱. خلاصهٔ مدیریتی

| سنجه | آغاز فاز | پایان فاز |
|---|---|---|
| کل تست‌ها | ۲٬۶۸۷ | **۲٬۷۷۵** |
| شکست | ۱۲ | **۰** |
| تطبیق‌های ۱۲ ماهه | منطبق | **منطبق** |
| مسیرهای مالی با Idempotency سمت سرور | ۱۳ | **۱۷** |
| Migrationهای منتظر استقرار | ۲ | **۴** |

از ۱۳ موردِ باز:

| وضعیت | موارد |
|---|---|
| **FIXED** (۹) | P1-01، P1-02، P1-04، P2-01، P3-A، P3-B، P3-C، ۱۲-A، ۱۲-C، ۱۲-E، ۱۲-F |
| **NOT FIXED — مستند** (۷) | P1-03، **P1-05**، P2-02، P2-03، P4-01، P4-02، ۱۲-B، ۱۲-D |

و مستقل از فهرست بالا: **۱۲ تستِ شکستهٔ قدیمی همگی ریشه‌یابی و رفع شدند** — ۹ مورد با
اصلاحِ خودِ پیاده‌سازی، ۳ مورد با به‌روزرسانیِ مستندِ تست پس از تغییرِ قراردادِ UI.
**هیچ تستی حذف یا ضعیف نشد.**

### چهار چیزی که واقعاً ریسک تولید را پایین آورد

1. **قفل دورهٔ عملیاتی (P1-01)** — دفتری که ماندهٔ واقعی طرف‌حساب‌ها را می‌سازد حالا
   مفهومِ «ماه بسته» دارد، و قاعده در `SaveChanges` اجرا می‌شود، پس هیچ سرویس، ایمپورت
   یا اسکریپتی نمی‌تواند بی‌صدا دورش بزند.
2. **یکسان‌سازیِ canonical متن افغانی (P1-04)** — `RWB-12345` ≡ `RWB-۱۲۳۴۵` ≡
   `RWB-١٢٣٤٥`. اثبات‌شده روی مسیر واقعی: `Probe08` حالا با انتظارِ معکوس سبز است.
3. **هشت اسکنر تطبیقِ فقط‌خواندنی (P1-02 + ۱۲-F)** — یتیم، گم‌شده، تکراری، موجودی منفی،
   سهم ناسازگار، بازهٔ هم‌پوشان، شراکتیِ بی‌سهم، کلید غیر canonical.
4. **دو داده‌ای که بی‌صدا از کاربر پنهان می‌شد، بازگردانده شد** — تاریخچهٔ رویدادهای
   بارگیری و زنجیرهٔ Split/Merge حمل (جزئیات در بخش ۷).

### دو نقصِ جدی که فقط دیتابیس واقعی نشانشان داد

هر دو **پیش از پایان فاز پیدا و رفع شدند** و ارزشِ اصرار بر اجرای واقعی را نشان می‌دهند:

* **`AddColumn "xmin"`** — Scaffold خودکارِ EF می‌خواست ستونِ **سیستمیِ** PostgreSQL را
  اضافه کند؛ اجرای آن استقرار را می‌شکست.
* **`42703: column p.xmin does not exist`** — الگوی هم‌زمانیِ `LoadingReceipt` روی
  موجودیت‌های مالی **کار نمی‌کند** و مسیرِ ثبتِ بارگیری را کاملاً از کار انداخت.
  در نتیجه P1-05 عمداً **NOT FIXED** ثبت شد (بخش ۲).

---

## ۲. موارد P1

### PTG-P1-01 — قفل دوره روی دفتر عملیاتی — **FIXED**

**ریشه:** `PeriodGuard` فقط داخل `AccountingPostingService` بود و آن ماژول با
`Accounting.Enabled = false` خاموش است. `LedgerEntries` هیچ مفهومی از دورهٔ بسته نداشت.

**تصمیم معماری:** ماژول حسابداریِ خاموش لمس نشد. کوچک‌ترین مدلِ مستقل: یک واترمارک
«تا این تاریخ بسته است» با تاریخچه — نه تقویمِ کامل.

| قطعه | نقش |
|---|---|
| `Models/Entities/OperationalPeriodLock.cs` | یک سطر برای هر بستن/بازکردن؛ قفلِ مؤثر = تازه‌ترین سطر فعال |
| `Services/OperationalPeriod/OperationalPeriodGuard.cs` | **تنها** مرجعِ «آیا در این تاریخ می‌شود سند زد؟» |
| `Services/OperationalPeriod/OperationalPeriodScope.cs` | دامنهٔ قفل: ۱۳ نوع سند و ستونِ تاریخِ تجاریِ هرکدام |
| `ApplicationDbContext.EnforceOperationalPeriodLock()` | پشتوانهٔ نهایی در `SaveChanges` |
| `Services/OperationalPeriod/BusinessRuleExceptionFilter.cs` | ترجمهٔ خطا به دری |
| `Controllers/OperationalPeriodLocksController.cs` + View | بستن/بازکردن با دلیل اجباری و Audit |

**عبورِ استثنایی:** فقط با Permission صریح `PostToClosedOperationalPeriod` (مثل
`PostToSoftLockedPeriod` از نقش استنتاج نمی‌شود)، با دلیل اجباری، و **بدون Audit اصلاً
انجام نمی‌شود**. دامنهٔ عبور همان یک درخواست است (روی `DbContext`، نه ثابت سراسری).

**پیام دری:**
> «دوره مالی این تاریخ بسته شده است و ثبت یا تغییر سند در این دوره مجاز نیست. (سند مصرف به تاریخ 2026-02-10؛ دوره تا 2026-03-31 بسته است.)»

**سازگاری:** تا وقتی هیچ قفلی ثبت نشده — یعنی وضعیت سیستم بلافاصله پس از استقرار —
رفتار **دقیقاً** مثل قبل است. اثباتِ عملی: کلِ مجموعهٔ ۲٬۷۷۵ تستی و شبیه‌سازیِ ۱۲ ماهه
با این کد سبزند و هیچ‌کدام قفلی نمی‌سازند.

**تست‌ها:** `OperationalPeriodLockTests` — ۱۴ تست، همه سبز.

---

### PTG-P1-02 — یکپارچگیِ مبدأ دفتر کل — **FIXED (با محدودیتِ صادقانه)**

**چرا FK واقعی ساخته نشد:** `(SourceType, SourceId)` چندریختی است — یک ستون به ده‌ها
جدول اشاره می‌کند. FK جعلی یا فقط به یک جدول می‌خورد (بقیه را می‌شکند) یا نیازمند
جمع‌کردن همهٔ اسناد در یک جدول است (بازنویسیِ کلِ حسابداری). گزینه‌های A/B/C/D ارزیابی و
**ترکیبِ B+C** انتخاب شد.

**سه لایه:**
1. اعتبارسنجیِ لحظهٔ نوشتن — کلیدهای خارجی `Restrict` و مسیرهای کنترل‌شدهٔ ثبت (موجود).
2. قفلِ دوره (P1-01) — ویرایشِ خارج از مسیر را در گذشته می‌بندد (**تازه**).
3. `LedgerIntegrityReconciliationService` — تطبیقِ فقط‌خواندنی (**تازه**).

| کد اسکنر | چه چیزی را پیدا می‌کند |
|---|---|
| `LEDGER-ORPHAN` | لجرِ بی‌سند (Sale، Expense، Loading، دو Transfer، و هر ۱۳ نوع Payment) |
| `LEDGER-MISSING` | فروش/مصرف بدون لجر |
| `LEDGER-DUPLICATE` | یک سند با بیش از یک لجر |
| `INVENTORY-NEGATIVE` | موجودی پایانیِ منفی |
| `PARTNER-SHARE-SUM` | بازهٔ سهم که ۱۰۰٪ نمی‌شود |
| `PARTNER-PERIOD-OVERLAP` | بازه‌های سهمِ هم‌پوشان |
| `PARTNERSHIP-WITHOUT-SHARES` | قرارداد شراکتی بدون هیچ سطر سهم |
| `IMPORT-KEY-NON-CANONICAL` | کلید ایمپورت غیر canonical + **برخوردهای احتمالی** |

**محدودیتِ باقی‌مانده، بی‌پرده:** یک `DELETE` خامِ مستقیم روی دیتابیس همچنان می‌تواند
لجرِ یتیم بسازد (شبیه‌سازیِ ۱۲ ماهه همین را به‌عنوان `SIM-LED-02` گزارش می‌کند). چیزی جز
FK جلویش را نمی‌گیرد و FK برای مدل چندریختی ممکن نیست. **آنچه تغییر کرده این است که
دیگر بی‌صدا نمی‌ماند.**

**تست‌ها:** `LedgerIntegrityReconciliationTests` — ۱۳ تست، شامل بازتولیدِ دقیقِ `Probe06`
و کنترلِ مثبتِ «دادهٔ سالم ⇒ صفر یافته».

---

### PTG-P1-03 — تجمیع نوشتنِ دفتر کل — **NOT FIXED (عمدی)**

**تصمیم:** این مورد یک refactor روی ~۱۸ نقطهٔ نوشتنِ دفتر کل است با شرطِ «خروجی عددی
باید دقیقاً یکسان بماند». انجامِ آن در همین فاز — کنار قفلِ دوره، تغییر مدل، و دو
Migration تازه — یعنی جابه‌جا کردن همهٔ مسیرهای مالی هم‌زمان با تغییرِ قواعدشان.
هر رگرسیونی در آن ترکیب، منشأیابی‌اش تقریباً ناممکن می‌شد.

**شواهد که این یک باگ فعال نیست:** شبیه‌سازیِ ۱۲ ماهه (همین اجرا) نشان داد جمعِ ماهانهٔ
فروش/مصرف با لجر در **هر ۱۲ ماه** منطبق است، لجرِ یتیم = ۰، لجرِ گم‌شده = ۰،
لجرِ تکراری = ۰. ریسک **ساختاری و آینده‌نگر** است، نه فعال.

**برنامهٔ فاز بعد:**
1. `ILedgerPostingService` با ورودی typed (SourceType، SourceId، Side، مبلغ، ارز، نرخ اعمال‌شده، ارجاع طرف/قرارداد، گِردکردن، Idempotency، متادیتای برگشت).
2. مهاجرت به ترتیب: Expense → Payment → Sale → Loading → Settlements → بقیه.
3. پس از هر مسیر: snapshot طلایی از `LedgerEntries` پیش/پس و مقایسهٔ سطر-به-سطر.
4. `LEDGER-DUPLICATE` و `LEDGER-MISSING` (که همین فاز ساخت) به‌عنوان نگهبانِ خودکارِ مهاجرت.

---

### PTG-P1-04 — یکسان‌سازی متن و ارقام افغانی — **FIXED**

**ریشه:** `LoadingImportKey.Normalize` فقط فاصله و بزرگی/کوچکی حروف را یکسان می‌کرد.

**`Helpers/AfghanTextNormalizer.cs` — تنها مرجع:**

* ارقام فارسی (U+06F0..U+06F9) و عربی-هندی (U+0660..U+0669) → لاتین
* `ي ى ۍ ئ`→`ی` · `ك`→`ک` · `ة ۀ`→`ه` · `أ إ آ ٱ`→`ا` · `ؤ`→`و`
* حذف ZWNJ/ZWJ، LRM/RLM، embedding/isolate، BOM، تتویل، اعرابِ عربی
* `NormalizationForm.FormKC` برای شکل‌های نمایشیِ عربی و عرض-کامل
* یکدست‌کردنِ فاصله + trim + حروف بزرگِ لاتین

**تفکیکِ صریحِ نمایش از هویت — مهم‌ترین قاعدهٔ طراحی:**

| متد | کاربرد |
|---|---|
| `CanonicalKey` | **فقط** هویت/تشخیص تکراری |
| `NormalizeForSearch` | مقایسهٔ جستجو |
| `NormalizeText` | مقایسهٔ متن توصیفی |
| `NormalizeDigits` | فقط ارقام |
| `TryParseDecimal` | عددِ نوشته‌شده با ارقام/جداکنندهٔ فارسی-عربی |

**مقدارِ نمایشیِ ذخیره‌شده هرگز تغییر نمی‌کند.** نامِ آدم‌ها و شرحِ اسناد همان‌طور که
کاربر نوشته می‌مانند.

**سازگاری عقب‌رو:** برای متنِ کاملاً لاتین خروجی **دقیقاً** همان رفتار قبلی است. خط تیره
و اسلش عمداً حذف نشدند (`RWB-123` و `RWB123` می‌توانند دو سند واقعی باشند).

**قاعدهٔ مهاجرت — رعایت شد:** هیچ Backfill خودکاری نوشته نشد. اسکنرِ
`IMPORT-KEY-NON-CANONICAL` هم سطرهای غیر canonical و هم **برخوردها** را گزارش می‌کند.
**هیچ رکوردی بی‌صدا ادغام نمی‌شود.**

**اثبات روی مسیر واقعی:** `Probe08` — که پیش‌تر نامش
`Persian_Digits_Defeat_The_Loading_Duplicate_Key` بود و *وجودِ* باگ را pin می‌کرد — به
`Probe08_Persian_And_Arabic_Digits_Produce_The_Same_Loading_Key` بازنویسی شد و سبز است،
به‌علاوهٔ دو ادعای معکوس که ثابت می‌کنند یکسان‌سازی **دو سند واقعاً متفاوت را ادغام نمی‌کند**.

**تست‌ها:** `AfghanTextNormalizerTests` (۲۵) + `Probe08`.

**پوششِ ناقصِ صادقانه:** یکسان‌سازی به **هویت/ضدتکرارِ ایمپورت** وصل شد (همان جایی که
نقص تأییدشده بود). به **جستجوی متنِ ذخیره‌شده** وصل نشد: جستجو روی ستونِ خامِ دیتابیس
اجرا می‌شود، پس canonical کردنِ فقط عبارتِ جستجو چیزی را بهتر نمی‌کند و راه‌حلِ درست یک
ستون/ایندکسِ محاسبه‌شده است — یک تغییر schema که دامنهٔ این فاز نبود.

---

### PTG-P1-05 — محافظ Lost Update — **NOT FIXED (بازتولید شد، عمداً برگردانده شد)**

این تنها موردی است که **پیاده‌سازی شد، شکست خورد، و آگاهانه پس گرفته شد.** روایت کامل،
چون دقیقاً همان چیزی است که فاز ۳ خواسته بود («FIRST reproduce this with real EF contexts»):

1. الگوی موجودِ پروژه (`LoadingReceipt`: `uint RowVersion` + `IsRowVersion()` که Npgsql
   به ستونِ سیستمیِ `xmin` نگاشت می‌کند) روی ۸ موجودیت اعمال شد.
2. **نقص اول:** Scaffold خودکارِ EF یک `AddColumn<uint>(name:"xmin", type:"xid")` برای
   هشت جدول تولید کرد. `xmin` ستونِ **سیستمیِ** PostgreSQL است و آن دستور با
   *"column name xmin conflicts with a system column name"* شکست می‌خورد. **اجرای آن
   Migration استقرار را می‌شکست.** دستی حذف شد.
3. **نقص دوم — تعیین‌کننده:** روی SQLite (که تست‌ها استفاده می‌کنند) نگاشت با
   `NOT NULL constraint failed: Contracts.RowVersion` می‌شکست، چون SQLite معادلِ `xmin`
   ندارد. با `if (Database.IsNpgsql())` محدود شد.
4. **نقص سوم — قاطع:** روی **PostgreSQL واقعی**، `Probe01b` (ثبت بارگیری) کاملاً از کار
   افتاد: `42703: column p.xmin does not exist`. دلیل: `xmin` فقط روی **جدولِ پایه**
   وجود دارد، نه روی **جدولِ مشتق**؛ و EF برای این موجودیت‌ها کوئری‌هایی با subquery
   می‌سازد. یعنی الگوی `LoadingReceipt` صرفاً به‌خاطر شکلِ سادهٔ کوئری‌هایش کار می‌کند.

**تصمیم:** هر ۸ نگاشت و هر ۸ ستونِ `RowVersion` **حذف شدند**. جایگزینی حدسی ساخته نشد.
شکستن مسیرِ ثبتِ بارگیری برای رفعِ یک ریسکِ نظری، معاملهٔ بدی است.

**آنچه از این کار ماند (و ارزشمند است):** ترجمهٔ `DbUpdateConcurrencyException` به پیام
دری در `BusinessRuleExceptionFilter`، تست‌شده و آماده — به‌محض اینکه نشانهٔ هم‌زمانیِ
درست اضافه شود، پیام از قبل آماده است:
> «این رکورد توسط کاربر دیگری تغییر کرده است. اطلاعات جدید را دوباره باز کنید.»

**برنامهٔ درستِ فاز بعد:** ستونِ نسخهٔ **صریح** (`bigint`) که با یک تریگرِ
`BEFORE UPDATE` افزایش می‌یابد — افزودنی، مستقل از شکلِ کوئری، و قابل آزمودن
موجودیت‌به‌موجودیت با دو `DbContext` واقعی.

**ریسکِ باقی‌مانده، بی‌پرده:** دو کاربر که هم‌زمان یک پرداخت/فروش/مصرف را ویرایش کنند،
آخری بی‌صدا برنده می‌شود. این وضعیت **از قبل هم همین بود** و در این فاز بدتر نشد.

---

## ۳. موارد P2

### PTG-P2-01 — ایمنی حذف شریک — **FIXED**

`EvaluatePartnerAsync` فقط `ContractPartners` را می‌دید. حالا هفت ارجاع:

`ContractPartners` · `PaymentTransaction.PaidByPartnerId` ·
`PartnerSettlement.From/ToPartnerId` · `Contract.SaleProceedsHolderPartnerId` ·
`AssetOwnershipShare.PartnerId` · `AssetRentShare.PartnerId` ·
`AssetRentTransaction.ChargedToPartnerId`

**به‌علاوه** یک `catch (DbUpdateException)` نهایی در `PartnersController.Delete`:
کلیدهای خارجیِ `Restrict` آخرین نگهبان‌اند، و اگر روزی رابطهٔ تازه‌ای از قلم بیفتد، کاربر
پیام دری می‌بیند نه خطای ۵۰۰. موجودیت به `Unchanged` برگردانده می‌شود تا ChangeTracker
آلوده نماند.

**تست‌ها:** `PartnerDeleteSafetyTests` — ۱۳ تست، هر هفت وابستگی جداگانه، هر دو سمتِ
تسویه، «شریکِ بدون ارجاع همچنان حذف می‌شود»، «ارجاع به شریک دیگر این یکی را قفل نمی‌کند».

### PTG-P2-02 — تجربهٔ خطای ایمپورت اکسل — **NOT FIXED (عمدی)**

یک قابلیتِ تازه است (صفحهٔ پیش‌نمایش + حالتِ «فقط سطرهای سالم»)، نه یک نقصِ ایمنی: رفتار
فعلی «همه یا هیچ» **امن** است و فقط ناخوشایند.

اولویت این فاز، نقص‌هایی بود که بی‌صدا داده را خراب می‌کنند. **آنچه انجام شد و
پیش‌نیازِ همین مورد است:** با P1-04 و اسکنرِ `IMPORT-KEY-NON-CANONICAL`، کلیدِ ضدتکرار
حالا واقعاً کار می‌کند — یعنی شرطِ ایمنیِ هر ایمپورتِ جزئی از قبل برقرار است.

### PTG-P2-03 — جریان اصلاح فروش — **NOT FIXED (عمدی)**

جریانِ درخواست‌شده (Cancel/Reverse → فروشِ جایگزین با هویت مستقل → پیوند دوطرفه)
هم‌زمان حرکت موجودی و ردیف دفتر کل را برمی‌گرداند — یعنی یک زیرسیستمِ تازه روی دو
حساس‌ترین مسیر سیستم، در همان فازی که قفلِ دوره و دو تریگر تازه اضافه شده است.

**وضعیت فعلی امن است:** `SalesController.Edit` جز یادداشت چیزی نمی‌پذیرد، پس امروز هیچ
خرابیِ خاموشی از این مسیر ممکن نیست.

**آمادگی‌ای که این فاز فراهم کرد:** قفلِ دوره، Idempotency، و اسکنرهای تطبیق — سه مورد
از چهار پیش‌شرطی که خودِ فاز ۸ لازم دانسته بود (چهارمی، محافظ هم‌زمانی، در P1-05 مستند شد).

---

## ۴. موارد P3

### P3-A — نمایانیِ تاریخ گذشته/آینده — **FIXED**

`Helpers/TransactionDateNotice.cs` + محافظِ کوچکی در `wwwroot/js/core.js` روی
`input[type=date][data-ptg-date-guard]`.

* **هیچ چیز مسدود نمی‌شود** — ثبتِ عقب‌تاریخ در کارِ روزمره قانونی است. اجرا با نگهبانِ
  موجودی (P0-02) و قفلِ دوره (P1-01) است.
* پیام **تعداد روزِ اختلاف** را می‌گوید تا «۲۰۲۵ به‌جای ۲۰۲۶» فوراً غیرعادی به نظر برسد.
* **«امروز» از ساعتِ کاری کابل رندر می‌شود، نه از ساعتِ مرورگر.**
* وصل‌شده به: Sales، Expenses، Payments، LossEvents، Dispatch (فرم‌های Create).

**تست‌ها:** `TransactionDateNoticeTests` (۱۱) شامل نگهبانِ ساختاری روی هر پنج فرم و `core.js`.

### P3-B — ممیزیِ Idempotency باقی‌مانده — **FIXED**

ممیزیِ همهٔ اکشن‌های `[HttpPost]`. چهار مسیرِ فعالِ دارای اثر مالی/موجودی بدون محافظ سرور:

| Purpose | چه چیزی می‌ساخت |
|---|---|
| `ThreeWaySettlement.Confirm` | ۱ تسویه + **۲ ردیف دفتر کل** (فقط هشدارِ heuristic داشت) |
| `Transport.FromReceipt` | حملِ مستقیم از رسید |
| `Transport.Continue` | انتقال/ادغام حمل |
| `Transport.SettleFreight` | **مصرفِ کرایهٔ واقعی** |

`TransportsController.Create` (POST) عمداً دست‌نخورده: فقط مسیریابی می‌کند و چیزی
نمی‌نویسد. `SarrafSettlements` و `ContractBalanceTransfers` همچنان در کد غیرفعال‌اند.

`FormIdempotencyCoverageTests` گسترش یافت (۴۱ تست) و هر دو شکلِ `_formTokens.Stamp(` و
`_formTokens?.Stamp(` را می‌شناسد.

### P3-C — کیفیت اعتبارسنجی دری — **FIXED**

`BusinessRuleExceptionFilter` (فیلتر سراسری) سه خطای «قاعدهٔ کسب‌وکار» را ترجمه می‌کند و
جزئیاتِ فنی را فقط لاگ:

| خطا | آنچه کاربر می‌بیند |
|---|---|
| `OperationalPeriodLockedException` | پیام قفلِ دوره با نامِ سند، تاریخ و دورهٔ بسته |
| `DbUpdateConcurrencyException` | «این رکورد توسط کاربر دیگری تغییر کرده است…» |
| `PostgresException` / `DbUpdateException` | پیام عمومی دری، **بدون نامِ Constraint** |

خطاهای غیرمرتبط عمداً دست‌نخورده رد می‌شوند تا واقعاً دیده و اصلاح شوند (تست‌شده).

---

## ۵. موارد P4 — تصمیمِ مبتنی بر اندازه‌گیری

هر دو مورد **اندازه‌گیری شدند** (بخش ۲۲) و هر دو **زیر بودجه** بودند:

| مسیر | اندازه‌گیری این فاز | بودجه |
|---|---|---|
| گردش حساب مشتری، بدون فیلتر تاریخ، ۳۰۰k لجر | ۲٬۴۰۸ ms | ۴٬۰۰۰ |
| صفحهٔ عمیق دفتر کل (offset ۲۵۰٬۰۰۰) | ۲۲۲ ms | ۳٬۰۰۰ |

### PTG-P4-01 — کارایی گردش حساب — **NOT FIXED**

بررسیِ کد نشان داد **اول دوره را نمی‌شود با یک `SUM` ساده گرفت**: جهتِ هر سند از لایهٔ
مرکزیِ CompanyFlow می‌آید و ممکن است با سمتِ حسابداری‌اش یکی نباشد (مثلاً «مصرف»)، و پس
از نگاشت، ادغامِ سهمِ مصارفِ محموله و بازیابیِ مبلغِ اصلیِ انتقالِ مانده هم انجام می‌شود.
تبدیلِ این به `SUM` سمتِ دیتابیس **معنا را عوض می‌کند، نه فقط سرعت را** — و قاعدهٔ
«Do not trade correctness for speed» صریح بود.

**پیشنهاد فاز بعد:** نگه‌داشتنِ نگاشتِ جهت ولی محاسبهٔ اول دوره با projection سبک‌تر
(بدون `Description`/`Reference`)، اندازه‌گیریِ پیش/پس، سپس صفحه‌بندیِ سمت سرور.

### PTG-P4-02 — صفحه‌بندی عمیق — **NOT FIXED (طبق دستور خودِ فاز)**

فاز گفته بود «Measure first… Otherwise document that current OFFSET remains acceptable
and leave it unchanged.» اندازه‌گیری شد: **۲۲۲ ms در برابر ۳٬۰۰۰ ms**. Keyset توجیه ندارد.
**بدون تغییر ماند** — دقیقاً همان چیزی که فاز خواسته بود.

---

## ۶. نگرانی‌های باقی‌مانده از گزارش P0

### ۱۲-A — نگهداریِ `ProcessedFormTokens` — **FIXED**

`ProcessedFormTokenRetentionService` + یک `BackgroundService` با همان الگوی
`BackupSchedulerHostedService` (**هیچ وابستگیِ زیرساختیِ تازه‌ای**).

* پنجرهٔ پیش‌فرض ۹۰ روز، **کفِ سختِ ۳۰ روز**؛ هیچ پیکربندی‌ای نمی‌تواند کوتاه‌ترش کند —
  وگرنه یک عددِ اشتباه در تنظیمات، محافظ ضدتکرارِ P0-01 را خاموش می‌کرد.
* توکنِ بدون `ConsumedAtUtc` **هرگز** حذف نمی‌شود (سنش معلوم نیست).
* هر اجرا حداکثر ۵٬۰۰۰ سطر، مرتب بر `ConsumedAtUtc` — کران‌دار و ایندکس‌دوست.
* توکنِ زنده پس از پاک‌سازی همچنان تکراری را می‌گیرد.

**تست‌ها:** `ProcessedFormTokenRetentionTests` — ۸ تست.

### ۱۲-B — فیلد «از تاریخ» در ویرایش سهم — **NOT FIXED (عمدی)**

یک قابلیتِ UI است، نه نقصِ ایمنی؛ و مستقیماً روی همان `ContractPartner`ای می‌نشیند که
فاز P0 تازه تاریخ‌دار کرده است. **آنچه انجام شد:** اسکنرِ `PARTNER-PERIOD-OVERLAP` ساخته
شد، پس نگهبانِ «بازهٔ هم‌پوشان» برای فاز بعد از قبل آماده است.

### ۱۲-C — معناشناسیِ نمایشِ سهم — **FIXED**

`ContractPartnerShareHistory.SharePeriodCount(contractId)` اضافه شد؛
`PartnershipPartnerTotals` دو عضو گرفت: `SharePeriodCount` و `SpansMultipleSharePeriods`.

در `Views/PartnershipStatement/Index.cshtml` برای قراردادِ چندبازه‌ای:

> سهم مفاد {شریک} — **سهم فعلی: ۸۰٪**
> *محاسبهٔ تاریخی شامل ۲ بازهٔ سهم است*

قراردادِ تک‌بازه — یعنی همهٔ دادهٔ موجود پس از Backfill فاز P0 — **دقیقاً مثل قبل**.
**هیچ عددی تغییر نکرد؛ فقط برچسبِ گمراه‌کننده رفع شد.**

### ۱۲-D — محدودیتِ دوره‌ایِ بهای خرید — **NOT FIXED (بررسی شد، حدس زده نشد)**

**بررسی:** تقسیمِ مفاد بین بازه‌های سهم بر پایهٔ **عایدِ فروشِ هر بازه** است، چون
`IPurchaseAggregationService` تاریخ‌دار نیست. بهای خرید از بارگیری/رسید می‌آید که تاریخِ
خودشان را دارند، ولی نگاشتِ «کدام بارگیری به کدام فروش» از مسیرِ تخصیص می‌گذرد؛ ساختنِ
مدلِ تازه یعنی بازطراحیِ تجمیعِ بهای خرید — همان چیزی که صورت‌مسئله گفته بود
«If it requires a risky accounting redesign, DO NOT guess.»

**محدودیتِ دقیق، برای ثبت:** برای قراردادی با **بیش از یک بازهٔ سهم**، مفاد به نسبتِ
عایدِ فروشِ هر بازه تقسیم می‌شود و بهای خرید تاریخ‌دار نیست. برای قراردادِ **تک‌بازه**
(همهٔ دادهٔ موجود) نتیجه دقیقاً برابرِ فرمولِ قبلی است — و شبیه‌سازیِ ۱۲ ماههٔ همین اجرا
تأیید کرد که سهم مفادِ ۴ جفت شریک منطبق است.

### ۱۲-E — نگهبانِ تبدیلِ نوع مالکیت قرارداد — **FIXED**

**شکاف:** تریگرِ P0-04 روی `ContractPartners` بود، پس تبدیلِ مستقیمِ «شخصی → شراکتی»
بدون لمسِ هیچ سطر شریکی اصلاً تریگر را شلیک نمی‌کرد.

**Migration `AddContractOwnershipShareGuard`** — CONSTRAINT TRIGGER تعویق‌دار روی
خودِ `Contracts`:

* هر قرارداد شراکتی که سطر سهم دارد، باید در **هر بازه** دقیقاً ۱۰۰٪ باشد — مستقل از
  اینکه تغییر از سمت قرارداد آمده یا از سمت شرکا.
* **UPDATE** ای که قرارداد را به شراکتی تبدیل کند و هیچ سهمی نباشد ⇒
  `PTG_PARTNERSHIP_WITHOUT_SHARES` (`23514`).
* **INSERT** عمداً مستثناست: ایمپورت‌هایی وجود دارند که قرارداد و شرکا را در دو تراکنش
  جدا می‌نویسند. آن حالت با اسکنرِ `PARTNERSHIP-WITHOUT-SHARES` پیدا می‌شود.

`ContractsController` پیام دری برای این خطا اضافه کرد.

**اثباتِ اجرا:** fixture شبیه‌سازی همهٔ Migrationها را روی دیتابیسِ تازه اجرا می‌کند و
۶۰ تستِ Simulation سبزند ⇒ **SQL این تریگر روی PostgreSQL واقعی کامپایل و اجرا شده است.**

### ۱۲-F — اسکنرهای دادهٔ تاریخیِ نامعتبر — **FIXED**

هر هشت مورد ساخته و تست شدند (جدول بخش P1-02). همه فقط‌خواندنی‌اند.

---

## ۷. دوازده تست شکستهٔ قبلی — ریشه، تصمیم، وضعیت

**هیچ‌کدام «تستِ اشتباه» نبودند؛ همه نتیجهٔ یک بازطراحیِ UI نیمه‌تمام بودند.**

| # | تست | ریشهٔ واقعی | تصمیم | وضعیت |
|---|---|---|---|---|
| ۱ | `MasterDataCleanupTests.Sidebar_Exposes_…` | commit `4ce2071` تستِ تبِ `("Transports","Create")` را افزود ولی تب هرگز به `_SectionTabs.cshtml` اضافه نشد؛ `TransportsController.Create` در کد **هست** | **اصلاح پیاده‌سازی** | ✅ |
| ۲ | `PartnerProfileTests.Ledger_HasTheAccountantsColumns` | commit `077bf66` ستون‌های «مقدار MT» و «نرخ» را حذف کرد، در حالی که سرویس همچنان `QuantityMt`/`UnitPriceUsd` را می‌سازد | **اصلاح پیاده‌سازی** | ✅ |
| ۳ | `PartnerProfileTests.QuantityAndRate_…` | همان + helper گمشدهٔ `Optional(...)` که «—» را به‌جای صفرِ ساختگی چاپ می‌کند | **اصلاح پیاده‌سازی** | ✅ |
| ۴ | `DetailsP0RegressionTests.Loading_Details_Does_Not_Truncate_Lists_With_Take5` | صفحه به پوستهٔ خطی رفت و `TimelineLimit = 4` گرفت: بارگیری با ۱۲ رسید فقط ۴ رویداد نشان می‌داد و **بقیه بی‌صدا دور ریخته می‌شد** | **هر دو**: `_DetailSecondary` رویدادهای قدیمی‌تر را به یک disclosure می‌برد + تست به قراردادِ تازه به‌روز شد | ✅ |
| ۵ | `DetailsP0RegressionTests.ShipmentPnl_Details_Uses_Single_Categorisation_Source` | View یک `ExpenseBucketUsd(...)` محلی داشت که کارِ `ShipmentExpenseCategorizer.TotalFor` را تکرار می‌کرد | **اصلاح پیاده‌سازی** | ✅ |
| ۶ | `AkDetailV2StructureTests.Operations_Details_…` | (الف) `ShipmentPnl/Details` هرگز به ترکیبِ خطی تبدیل نشده بود؛ (ب) `summaryItems` در View گمرک به `mainItems` تغییر نام داده بود | (الف) **اصلاح پیاده‌سازی**؛ (ب) **به‌روزرسانیِ مستندِ تست** (فقط نامِ متغیر) | ✅ |
| ۷ | `OperationsLinearDetailStructureTests.All_Operations_Details_…` | همان (الف) | **اصلاح پیاده‌سازی** | ✅ |
| ۸ | `OperationsLinearDetailStructureTests.Shipment_Removes_The_Per_Tab_Kpi_Card_Dashboard` | همان (الف) | **اصلاح پیاده‌سازی** | ✅ |
| ۹ | `ShipmentPnlControllerTests.Finance_Tab_…` | جدولِ «جزئیات هزینه‌ها بر اساس دسته» در `4ce2071` حذف شده بود ولی **کامنتی که نامش را می‌برد** جا مانده بود | **اصلاح پیاده‌سازی** (کامنتِ نادرست؛ تست دست‌نخورده) | ✅ |
| ۱۰ | `ContractJourneyViewStructureTests.Loading_Receipt_Form_…` | عنوان‌های فرم کوتاه شدند؛ **ترتیبی که تست pin می‌کرد دست‌نخورده بود** | **به‌روزرسانیِ مستندِ تست** — لنگرها به `id`/`name` پایدار منتقل شدند | ✅ |
| ۱۱ | `ContractJourneyViewStructureTests.Operation_Record_Detail_Pages_…` | همان (الف) — `ShipmentPnl` نهمین صفحه بود | **اصلاح پیاده‌سازی** | ✅ |
| ۱۲ | `TransportWorkflowViewStructureTests.Details_Centres_The_Chain_…` | commit `1550151` بخشِ «مسیر حمل» (Split/Merge با لینکِ `TR-xxxx`) را حذف کرد و جایگزینی نگذاشت — CSSاش یتیم مانده بود. باری که بین دو موتر شکسته شده، **منبعش در هیچ صفحه‌ای دیده نمی‌شد** | **اصلاح پیاده‌سازی** — بخش زنجیره با همان markup و همان CSS بازگردانده شد | ✅ |

**جمع‌بندی: ۹ اصلاحِ پیاده‌سازی، ۳ به‌روزرسانیِ مستندِ تست، ۰ حذف، ۰ ضعیف‌سازی.**

یک تستِ سیزدهم (`ShipmentPnlControllerTests.Details_View_Uses_Vessel_Case_File_Sections_And_Trip_Actions`)
با تبدیلِ `ShipmentPnl` به ترکیبِ خطی در **تضادِ مستقیم** با
`Shipment_Removes_The_Per_Tab_Kpi_Card_Dashboard` قرار گرفت (یکی ۶ گریدِ کارت می‌خواست،
دیگری صفر). قراردادِ تازه‌تر برنده شد و بندهای متناقض به‌روز شدند؛ بندهای دیگرش — از جمله
«یک منوی خروجی مشترک» و ترتیبِ صفحه — **تقویت شدند، نه حذف**.

هیچ عددِ مالی در هیچ‌کدام از این ۱۲ اصلاح تغییر نکرد.

---

## ۸. فایل‌های تغییرکرده

### تازه — Production

```
Helpers/AfghanTextNormalizer.cs
Helpers/TransactionDateNotice.cs
Models/Entities/OperationalPeriodLock.cs
Services/OperationalPeriod/OperationalPeriodGuard.cs
Services/OperationalPeriod/OperationalPeriodScope.cs
Services/OperationalPeriod/BusinessRuleExceptionFilter.cs
Services/ProcessedFormTokenRetentionService.cs
Services/Reconciliation/LedgerIntegrityReconciliationService.cs
Controllers/OperationalPeriodLocksController.cs
Views/OperationalPeriodLocks/Index.cshtml
Migrations/20260829120839_AddOperationalPeriodLock.cs            (+ .Designer.cs)
Migrations/20260829120906_AddContractOwnershipShareGuard.cs      (+ .Designer.cs)
```

### تغییرکرده — Production

```
Data/ApplicationDbContext.cs                     قفل دوره در SaveChanges + DbSet + ایندکس
Program.cs                                       گارد دوره، فیلتر خطا، سرویس تطبیق، نگهداری توکن، Policy
Security/RoleAccessRules.cs                      دو Permission + CanManageOperationalPeriodLock + ناوبری
Security/AuthPolicies.cs                         OperationalPeriodAdmin
Helpers/LoadingImportKey.cs                      Normalize به AfghanTextNormalizer واگذار شد
Services/DeleteSafety/MasterDataDeleteSafetyService.cs   هفت ارجاع شریک
Services/PartyStatements/ContractPartnerShareHistory.cs  SharePeriodCount
Services/PartyStatements/PartnershipStatementService.cs  انتشار SharePeriodCount
Controllers/PartnersController.cs                catch نهایی DbUpdateException
Controllers/ContractsController.cs               پیام PTG_PARTNERSHIP_WITHOUT_SHARES
Controllers/ThreeWaySettlementController.cs      Idempotency
Controllers/TransportsController.cs              Idempotency ×۳
```

### Viewها و CSS/JS

```
Views/Shared/_SectionTabs.cshtml                 تب «ثبت حمل»
Views/Shared/Partials/_DetailSecondary.cshtml    رویدادهای قدیمی‌تر دیگر دور ریخته نمی‌شوند
Views/Partners/Details.cshtml                    ستون‌های «مقدار MT» و «نرخ» بازگشتند
Views/ShipmentPnl/Details.cshtml                 ترکیب خطی مشترک + منبع واحد دسته‌بندی
Views/InventoryTransportLegs/Details.cshtml      بخش «مسیر حمل» بازگشت + FormToken
Views/Transports/{FromReceipt,Continue}.cshtml   FormToken
Views/ThreeWaySettlement/Index.cshtml            FormToken
Views/PartnershipStatement/Index.cshtml          معناشناسی سهم چندبازه‌ای
Views/{Sales,Expenses,Payments,LossEvents,Dispatch}/Create.cshtml   data-ptg-date-guard
wwwroot/js/core.js                               initializeDateGuards
```

### تست‌ها

```
تازه:   AfghanTextNormalizerTests (۲۵)          OperationalPeriodLockTests (۱۴)
        PartnerDeleteSafetyTests (۱۳)            LedgerIntegrityReconciliationTests (۱۳)
        ProcessedFormTokenRetentionTests (۸)     TransactionDateNoticeTests (۱۱)
به‌روز: Simulation/ProductionRiskProbeTests (Probe08 با انتظار معکوس)
        Simulation/FormIdempotencyCoverageTests  DetailsP0RegressionTests
        ShipmentPnlControllerTests               AkDetailV2StructureTests
        ContractJourneyViewStructureTests
```

**هیچ فایل تستی حذف نشد.**

---

## ۹. Migrationهای ساخته‌شده

| # | نام | کار | تخریبی؟ | روی DB موقت اجرا شد؟ | روی Production؟ |
|---|---|---|---|---|---|
| ۱ | `20260829120839_AddOperationalPeriodLock` | جدول `OperationalPeriodLocks` + یک ایندکس | **خیر** — جدول خالی | ✅ بله | ❌ **خیر** |
| ۲ | `20260829120906_AddContractOwnershipShareGuard` | تابع + CONSTRAINT TRIGGER تعویق‌دار روی `Contracts` | **خیر** — فقط تابع/تریگر | ✅ بله | ❌ **خیر** |

هر دو در هر اجرای Simulation روی یک دیتابیسِ موقتِ تازه از صفر اجرا می‌شوند و ۶۰ تست
روی نتیجهٔ آن سبزند.

دو Migration فاز P0 (`AddContractPartnerShareSumGuard`، `AddContractPartnerEffectiveDating`)
هم همچنان روی Production **اجرا نشده‌اند**. **در مجموع ۴ Migration منتظرِ استقرار است.**

> **درسِ ثبت‌شده:** نسخهٔ اولِ Migration ۱ شاملِ `AddColumn<uint>(name:"xmin")` برای هشت
> جدول بود که استقرار را می‌شکست. آن مسیر کلاً کنار گذاشته شد (P1-05) و Migration از نو
> ساخته شد. Snapshot هم پس از آن به حالتِ سازگار با آخرین Migration فاز P0 بازگردانده شد.

---

## ۱۰. اثر روی schema

| تغییر | نوع |
|---|---|
| جدول `OperationalPeriodLocks` + ایندکس `(IsActive, LockedThroughDate)` | افزودنی |
| تابع `ptg_check_contract_ownership_shares()` + تریگر `TR_Contracts_OwnershipShares` | افزودنی |

**هیچ ستونی اضافه، حذف یا تغییر نوع نداد. هیچ سطری تغییر نکرد یا حذف نشد.**
(ایندکس‌های عمومی: ۵۹۴ → **۵۹۷**.)

## ۱۱. اثر بر رفتار مالی

* **بدون قفل فعال: صفر تغییر.** هیچ فرمول، گِردکردن، جهت یا نرخی عوض نشد — و شبیه‌سازیِ
  ۱۲ ماهه این را عدد‌به‌عدد تأیید کرد.
* پس از بستنِ دوره: ثبت/ویرایش/حذفِ اسنادِ آن دوره رد می‌شود (پیام دری) مگر با عبورِ
  Audit شدهٔ دارای Permission.
* `ThreeWaySettlement.Confirm` دیگر با Refresh دو تسویه و **چهار** ردیف دفتر کل نمی‌سازد.
* `Transport.SettleFreight` دیگر کرایه را دو برابر نمی‌کند.
* برچسبِ سهمِ شراکت برای قراردادِ چندبازه‌ای دیگر گمراه‌کننده نیست (**عدد تغییر نکرد**).

## ۱۲. اثر بر رفتار موجودی

* `Transport.FromReceipt` و `Transport.Continue` idempotent شدند.
* اسکنرِ `INVENTORY-NEGATIVE` هر scope با ماندهٔ پایانیِ منفی را فهرست می‌کند.
* نگهبانِ P0-02 دست‌نخورده و سبز است (`Probe03`، `Probe03b`، `Probe05`).
* تطبیقِ `StockService` با ریاضیِ حرکات روی ۶۰ scope: **اختلاف صفر**.

## ۱۳. اثر بر رفتار شراکت

* نگهبانِ «۱۰۰٪ در هر بازه» حالا از سمتِ `Contracts` هم اجرا می‌شود (۱۲-E).
* `SharePeriodCount`/`SpansMultipleSharePeriods` اضافه شد؛ **اعداد دست‌نخورده‌اند**.
* حذف شریکِ دارای هر نوع ارجاع پیام روشن می‌دهد نه خطای ۵۰۰.
* سهم مفادِ ۴ جفت شریک در شبیه‌سازیِ ۱۲ ماهه **منطبق**.

## ۱۴. نتایج هم‌زمانی

**محافظ اضافه نشد** — بخش P1-05. الگوی موجودِ پروژه روی این موجودیت‌ها بازتولید شد و
روی PostgreSQL واقعی شکست (`42703`)، پس آگاهانه برگردانده شد. پیام دریِ برخوردِ
هم‌زمانی ساخته و تست شده و آمادهٔ فازِ بعد است.

## ۱۵. پوشش Idempotency

۱۳ مسیرِ فاز P0 + ۴ مسیرِ تازه = **۱۷ مسیرِ ثبتِ مالی/عملیاتی** با محافظ سمت سرور.
`FormIdempotencyCoverageTests` (۴۱ تست) این را ساختاری pin می‌کند.
اثباتِ رفتاری: `Probe01`، `Probe01b`، `Probe02` سبزند.

## ۱۶. نتایج یکسان‌سازی ایمپورت

`RWB-12345` ≡ `RWB-۱۲۳۴۵` ≡ `RWB-١٢٣٤٥` — **اثبات‌شده در `Probe08`**.
`کشتي` ≡ `کشتی` · `می‌خواهم` ≡ `میخواهم` · BOM/RLM/تتویل/اعراب حذف می‌شوند.
متنِ لاتین بیت‌به‌بیت مثل قبل. هویت‌های واقعاً متفاوت متفاوت می‌مانند.
مهاجرتِ داده انجام **نشد** — به‌جایش گزارشِ برخورد (طبق قاعدهٔ صریحِ فاز).

## ۱۷. نتایج قفل دوره

۱۴ تست سبز. کلِ مجموعهٔ ۲٬۷۷۵ تستی و شبیه‌سازیِ ۱۲ ماهه با قفلِ غیرفعال (حالت پیش‌فرض)
بدون هیچ تغییر رفتاری سبزند.

## ۱۸. نتایج یکپارچگی دفتر کل

۸ اسکنر، ۱۳ تست سبز، شامل بازتولیدِ `Probe06`.
شبیه‌سازیِ ۱۲ ماهه: لجرِ یتیم **۰**، لجرِ گم‌شده **۰**، لجرِ تکراری **۰**.

---

## ۱۹. کارایی — پیش/پس (اندازه‌گیری‌شده)

### دادهٔ یک‌ساله (۵٬۷۰۰ لجر)

| مسیر | قبل از P0 | بعد از P0 | **بعد از سخت‌سازی** |
|---|---|---|---|
| `StockService.GetMovementSummaryAsync` | 78 ms | 43 ms | **16 ms** |
| `ProfitAndLossService.BuildCompanyAsync` | 121 ms | 83 ms | **23 ms** |
| `NegativeStockAnalysisService.AnalyzeAsync` | 64 ms | 102 ms | **83 ms** |
| صفحهٔ اول دفتر کل (۵۰ ردیف) | 8 ms | 16 ms | **6 ms** |
| `PartnershipStatementService.BuildAsync` | 22 ms | 30 ms | **24 ms** |

### حجم چندساله (۳۰۰٬۰۰۰ لجر / ۱۵۰٬۰۰۰ حرکت / ۶۰٬۰۰۰×۳ سند)

| مسیر | قبل از P0 | بعد از P0 | **بعد از سخت‌سازی** | بودجه |
|---|---|---|---|---|
| صفحهٔ اول دفتر کل | 54 ms | 31 ms | **62 ms** | 1,000 |
| صفحهٔ عمیق (offset 250k) | 357 ms | 243 ms | **222 ms** | 3,000 |
| شمارش کل دفتر کل | 154 ms | 157 ms | **131 ms** | 2,000 |
| گردش حساب مشتری (بدون فیلتر) | 3,978 ms | 1,931 ms | **2,408 ms** | 4,000 |
| گردش حساب تأمین‌کننده | 1,949 ms | 2,111 ms | **1,819 ms** | 4,000 |
| موجودی آزاد یک مخزن | 125 ms | 112 ms | **141 ms** | 1,500 |
| خلاصهٔ حرکات موجودی | 542 ms | 210 ms | **420 ms** | 5,000 |
| تحلیل موجودی منفی | 1,967 ms | 1,462 ms | **1,527 ms** | 8,000 |
| P&L شرکت | 251 ms | 301 ms | **286 ms** | 8,000 |
| لیست فروش صفحهٔ اول | 36 ms | 49 ms | **42 ms** | 1,500 |

**هیچ مسیری از بودجهٔ خود عبور نکرد و هیچ افت معناداری دیده نشد.** نوسان‌های ده‌ها
میلی‌ثانیه نویزِ بار ماشین است. قفلِ دوره یک `SELECT` کوچک و **کش‌شده به‌ازای هر
DbContext** اضافه می‌کند و در این اعداد قابل تشخیص نیست.

## ۲۰. تعداد کل تست‌ها

```
آغاز فاز (گزارش P0):   Failed: 12   Passed: 2675   Total: 2687
پایان فاز:             Failed:  0   Passed: 2775   Total: 2775   (4 m 42 s)
```

`dotnet build` — **0 Error**.

مجموعهٔ Simulation جداگانه: **60 / 60 سبز** (۲.۴ دقیقه).

## ۲۱. نتایج شبیه‌سازی ۱۲ ماهه (اجرای تازه)

| بررسی | نتیجه |
|---|---|
| Contracts / Loadings / Receipts | 80 / 1200 / 1200 |
| InventoryMovements / Sales / Expenses / Payments | 2820 / 1500 / 1500 / 1500 |
| TruckDispatches / LossEvents / LedgerEntries | 600 / 120 / 5700 |
| `StockService` در برابر ریاضیِ حرکات — ۶۰ scope | ✅ اختلاف صفر |
| ردیف لجر یتیم (Sale/Expense/Loading) | ✅ 0 / 0 / 0 |
| سند بدون لجر (فروش/مصرف/پرداخت) | ✅ 0 / 0 / 0 |
| لجر تکراری برای یک فروش | ✅ 0 |
| جمع ماهانهٔ فروش/مصرف در برابر لجر — هر ۱۲ ماه | ✅ منطبق |
| سهم مفاد شرکا — ۴ جفت | ✅ منطبق |
| ماندهٔ ۶ تأمین‌کننده | ✅ منطبق |
| سهم شراکت ناسازگار (per-period) | ✅ صفر |
| ثبت دوبارهٔ سند مالی (`Probe01`, `Probe01b`, `Probe02`) | ✅ فقط یکی ثبت می‌شود |
| موجودی منفی از ثبت عقب‌تاریخ (`Probe03`) | ✅ مسدود |
| ثبت عقب‌تاریخِ مجاز (`Probe03b`) | ✅ پذیرفته |
| بازنویسی سود شرکا (`Probe04`, `Probe04b`) | ✅ گذشته ثابت، آینده با درصد تازه |
| قفل هم‌زمانی مخزن (`Probe05`) | ✅ فروش بیش از موجودی ممکن نیست |
| دقت ارز (`Probe07`) | ✅ خطای رفت‌وبرگشت صفر |
| **ارقام فارسی/عربی در کلید ایمپورت (`Probe08`)** | ✅ **یک هویت** |
| سهم ≠ ۱۰۰٪ در لایهٔ داده (`Probe09*`) | ✅ رد می‌شود |

دو یافتهٔ باقی‌مانده، هر دو **شناخته‌شده و مستند**:

* `SIM-INV-04` — ۸ گودالِ گذرای منفی که خودشان ترمیم شده‌اند. **artifact مولدِ دادهٔ
  حجیم** است که حرکات را مستقیم می‌نویسد و از نگهبان عبور نمی‌کند؛ ضمناً با قاعدهٔ
  «ماندهٔ پایانی» (P0-02) این حالت عمداً مجاز است. مدرکِ واقعیِ اصلاح `Probe03` است که از
  `SalesController` عبور می‌کند.
* `SIM-LED-02` — نبودِ FK روی `LedgerEntries.SourceId`. همان محدودیتِ چندریختی که در
  P1-02 مستند شد و اکنون با هشت اسکنر **قابل کشف** است.

## ۲۲. نتایج شبیه‌سازی مقیاس (اجرای تازه)

ساخت ۳۰۰٬۰۰۰ لجر / ۱۵۰٬۰۰۰ حرکت / ۶۰٬۰۰۰ فروش / ۶۰٬۰۰۰ مصرف / ۶۰٬۰۰۰ پرداخت: ۵۶.۱ ثانیه.
جدول کامل در بخش ۱۹. **هیچ نقضِ Invariant در این اجرا دیده نشد.**

## ۲۳. محدودیت‌های شناخته‌شدهٔ باقی‌مانده

1. **P1-05 — بدون محافظ Lost Update.** دو کاربر که هم‌زمان یک سند مالی را ویرایش کنند،
   آخری بی‌صدا برنده می‌شود. **مهم‌ترین محدودیتِ باقی‌مانده.** وضعیت نسبت به قبل بدتر
   نشده؛ راه‌حلِ درست (ستونِ نسخهٔ صریح) در بخش P1-05 مشخص شده است.
2. **P1-03** — نوشتنِ دفتر کل همچنان در ~۱۸ نقطه پخش است (ریسکِ آینده، نه باگ فعال).
3. **`DELETE` خامِ مستقیم** روی دیتابیس می‌تواند لجرِ یتیم بسازد — اکنون قابل کشف.
4. **P2-02، P2-03** — هر دو امن ولی ناخوشایند.
5. **P4-01، P4-02** — اندازه‌گیری شدند و **زیر بودجه**اند؛ عمداً دست‌نخورده.
6. **۱۲-B، ۱۲-D** — انجام نشدند؛ محدودیتِ ۱۲-D دقیقاً ثبت شده است.
7. **جستجوی متنِ ذخیره‌شده** canonical نیست (فقط هویتِ ایمپورت). نیازمند ستون/ایندکسِ
   محاسبه‌شده.
8. **۴ Migration روی Production اجرا نشده‌اند.**
9. عبورِ استثنایی از دورهٔ بسته UI ندارد؛ Permission باید بیرون از برنامه اعطا شود
   (همان وضعیتِ `PostToSoftLockedPeriod` موجود).

## ۲۴. چک‌لیست استقرار تولید

**پیش از استقرار:**

1. **Backup کامل از `ptg_oil_system`.**
2. یک **کلون** از Production بسازید. مراحل ۳ تا ۶ روی کلون.
3. روی کلون: `LedgerIntegrityReconciliationService.RunAsync()` را **پیش از** Migration اجرا کنید.
   * `PARTNER-SHARE-SUM` — قراردادهای ناسازگار **قبل** از migration اصلاح شوند، وگرنه
     نخستین ویرایششان شکست می‌خورد.
   * `IMPORT-KEY-NON-CANONICAL` — برخوردها را دستی بررسی کنید؛ **هیچ ادغام خودکاری نکنید.**
   * `INVENTORY-NEGATIVE` — scopeهای منفی هر خروجِ تازه‌ای را رد می‌کنند تا اصلاح شوند.
4. روی کلون: `dotnet ef database update` و تأیید موفقیتِ هر ۴ Migration.
5. روی کلون: کلِ مجموعهٔ تست با `PTG_TEST_POSTGRES_ADMIN`. **انتظار: ۰ شکست.**
6. روی کلون: اسکنرها را دوباره اجرا کنید. **انتظار: همان نتیجهٔ مرحلهٔ ۳** (Migrationها
   هیچ داده‌ای را تغییر نمی‌دهند).

**استقرار:**

7. `Accounting.Enabled` را `false` نگه دارید. هیچ Pilot ای فعال نشود.
8. Migrationها را روی Production اجرا کنید (پس از Backup تأییدشده).
9. **قفلِ دوره را فوراً فعال نکنید.** بگذارید سیستم چند روز با رفتارِ قبلی کار کند، بعد
   نخستین ماهِ بسته را از `/OperationalPeriodLocks` ثبت کنید.
10. `PostToClosedOperationalPeriod` را فقط به یک نفر بدهید (یا به هیچ‌کس).
11. اولین اجرای پاک‌سازیِ توکن ۵ دقیقه پس از راه‌اندازی است؛ لاگ را ببینید.

**پس از استقرار:**

12. اسکنرهای تطبیق را **هفتگی** اجرا کنید.
13. لاگِ `BusinessRuleExceptionFilter` را رصد کنید.
14. تا وقتی P1-05 حل نشده، کاربران را آموزش دهید که یک سند مالی را هم‌زمان از دو جا
    ویرایش نکنند.

---

## PRODUCTION RELEASE GATE

> **IS PTG OIL SYSTEM READY FOR CONTROLLED PRODUCTION DEPLOYMENT?**

# **READY WITH DOCUMENTED LOW-RISK LIMITATIONS**

### توجیه بر پایهٔ شواهدِ اندازه‌گیری‌شده

**آنچه اثبات شد — با اجرا، نه با استدلال:**

| شاهد | نتیجه |
|---|---|
| `dotnet build` | 0 Error |
| کلِ مجموعهٔ تست روی PostgreSQL واقعی | **۲٬۷۷۵ / ۲٬۷۷۵ سبز، ۰ شکست** |
| ۱۲ شکستِ قدیمی | همه ریشه‌یابی و رفع؛ ۹ باگِ واقعیِ پیاده‌سازی بودند |
| شبیه‌سازیِ ۱۲ ماهه | همهٔ تطبیق‌ها دقیق: موجودی، لجر، ماهانه، شراکت، تأمین‌کننده |
| لجر یتیم / گم‌شده / تکراری | ۰ / ۰ / ۰ |
| ۱۷ Probe ریسک تولید | همه سبز — از جمله هر چهار invariant فاز P0 |
| مقیاس ۳۰۰k لجر | هر ۱۰ مسیر زیر بودجه |
| هر ۴ Migration | روی دیتابیسِ موقتِ تازه اجرا شدند؛ SQL تریگرها کامپایل شد |
| Production | **دست‌نخورده** |

**چرا «READY» خالی نیست:** یک محدودیتِ واقعی باقی است —
**P1-05: بدون محافظ Lost Update.** دو کاربر که هم‌زمان یک پرداخت را ویرایش کنند، آخری
بی‌صدا برنده می‌شود. این وضعیت **از قبل هم بود** و در این فاز بدتر نشد، ولی رفع هم نشد:
الگوی موجودِ پروژه روی این موجودیت‌ها آزموده شد، روی PostgreSQL واقعی شکست
(`42703: column p.xmin does not exist`) و آگاهانه پس گرفته شد تا مسیرِ ثبتِ بارگیری
نشکند. جایگزینِ حدسی ساخته نشد.

بقیهٔ موارد باز (P1-03، P2-02، P2-03، P4-01، P4-02، ۱۲-B، ۱۲-D) **ریسکِ ساختاری یا
تجربهٔ کاربری‌اند، نه ریسکِ خرابیِ داده** — و شبیه‌سازیِ ۱۲ ماههٔ همین اجرا نشان داد
اعدادِ امروز درست‌اند.

**«Controlled» یعنی:** چک‌لیست بخش ۲۴ به‌ترتیب اجرا شود — به‌ویژه اجرای اسکنرها روی یک
کلون **پیش از** Migration، و فعال‌نکردنِ فوریِ قفلِ دوره.

---

## تأیید ایمنی دیتابیس

* **`ptg_oil_system` دست‌نخورده است.** هیچ اتصال نوشتنی به آن برقرار نشد.
* `dotnet ef database update` روی Production **اجرا نشد**.
* تنها فرمان‌های EF، `migrations add` بودند که به دیتابیس وصل نمی‌شوند؛ برای احتیاط
  بیشتر `ConnectionStrings__DefaultConnection` حین آن‌ها روی نامِ موقتِ پیشونددارِ
  `ptg_oil_accounting_test_migrationscaffold` ست شد — آن دیتابیس هرگز ساخته نشد.
* همهٔ تست‌های یکپارچگی روی دیتابیس‌های موقتِ با پیشوند اجباری
  `ptg_oil_accounting_test_` ساخته و در پایان `DROP` شدند
  (`DatabaseSafetyGuard.EnsureIntegrationTestCreate/Use/DropAllowed`).
* هیچ رکورد واقعی مشتری خوانده، تغییر یا حذف نشد.
* `Accounting.Enabled` دست‌نخورده و همچنان `false` است. هیچ Pilot ای فعال نشد.
* هیچ چیزی deploy نشد.

**متوقف شدم. منتظر دستور صریح برای استقرار.**
