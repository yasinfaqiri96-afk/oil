@model PTGOilSystem.Web.Models.ShipmentPnl.ShipmentRecordEmptyCell
@* حالت خالی مشترک — داخل همان کارت و همان جدول، بدون alert خام. *@
<tr class="shipment-record-table__empty-row">
    <td colspan="@Model.ColumnCount">
        <div class="shipment-record-empty">
            <i class="bi @Model.Icon" aria-hidden="true"></i>
            <p>@Model.Text</p>
        </div>
    </td>
</tr>
