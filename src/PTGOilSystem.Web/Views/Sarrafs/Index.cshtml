@model SarrafIndexViewModel
@{
    ViewData["Title"] = "صراف‌ها";
    var returnUrl = $"{Context.Request.Path}{Context.Request.QueryString}";
    var payViaSarrafUrl = Url.Action("Create", "Payments", new { method = "sarraf" });
}

<div class="ak-list-page">

    <partial name="~/Views/Shared/Components/Ak/_AkPageHeader.cshtml"
             model="@((UiText.T(Context, "صراف‌ها", "Sarrafs"), true,
                       UiText.T(Context, "صراف جدید", "New sarraf"), "bi-plus-lg",
                       (string?)Url.Action("Create", new { returnUrl }), (string?)null,
                       UiText.T(Context, "پرداخت از طریق صراف", "Pay via sarraf"), "bi-currency-exchange", payViaSarrafUrl))" />

    <section class="ak-stat-grid mb-3" aria-label="@UiText.T(Context, "آمار صراف‌ها", "Sarraf statistics")">
        <vc:stat-card title="@UiText.T(Context, "کل صراف‌ها", "Total sarrafs")" value="@Model.Items.Count.ToString("N0")" unit="@UiText.T(Context, "صراف", "sarrafs")" avatar="exchange-rate"></vc:stat-card>
        <vc:stat-card title="@UiText.T(Context, "صراف‌های فعال", "Active sarrafs")" value="@Model.ActiveCount.ToString("N0")" unit="@UiText.T(Context, "صراف", "sarrafs")" avatar="rules-checklist"></vc:stat-card>
        <vc:stat-card title="@UiText.T(Context, "جمع سپرده‌شده", "Total charged")" value="@Model.TotalChargedUsd.ToString("#,0.##")" unit="USD" avatar="payments-out"></vc:stat-card>
        <vc:stat-card title="@UiText.T(Context, "مانده پرداختنی", "Payable balance")" value="@Model.TotalPayableUsd.ToString("#,0.##")" unit="USD" avatar="receivables"></vc:stat-card>
    </section>

    <partial name="~/Views/Shared/_AkSearchFilter.cshtml"
             model="@AkSearchFilterModel.SearchOnly("search", Model.Search, UiText.T(Context, "جستجو یا فیلتر نتایج...", "Search or filter results..."))" />

    <div class="ak-list">
        <div class="ak-table-wrap">
            <table class="ak-table">
                <thead>
                    <tr>
                        <th class="ak-col-check"><input type="checkbox" class="ak-check" aria-label="@UiText.T(Context, "انتخاب همه", "Select all")" /></th>
                        <th class="ak-col-grow">@UiText.T(Context, "نام", "Name")</th>
                        <th>@UiText.T(Context, "تماس", "Contact")</th>
                        <th>@UiText.T(Context, "تسویه‌ها", "Settlements")</th>
                        <th class="ak-col-spacer"></th>
                        <th class="ak-col-num">@UiText.T(Context, "مانده USD", "Balance USD")</th>
                        <th>@UiText.T(Context, "وضعیت", "Status")</th>
                        <th class="ak-col-actions"></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.Items)
                    {
                        <tr data-href="@Url.Action("Details", new { id = item.Id })">
                            <td class="ak-col-check"><input type="checkbox" class="ak-check" aria-label="@UiText.T(Context, "انتخاب", "Select")" /></td>
                            <td class="ak-col-grow"><partial name="~/Views/Shared/Partials/_PersonCell.cshtml" model="@((item.Name, (string?)Url.Action("Details", new { id = item.Id }), (string?)null))" /></td>
                            <td class="ak-num">@(string.IsNullOrWhiteSpace(item.PhoneNumber) ? "-" : item.PhoneNumber)</td>
                            <td>@item.SettlementCount.ToString("N0")</td>
                            <td class="ak-col-spacer"></td>
                            <td class="ak-num ak-col-num">@item.PayableUsd.ToString("N2")</td>
                            <td>
                                <span class="ak-status @(item.IsActive ?"is-active" : "is-inactive")">
                                    @(item.IsActive ? UiText.T(Context, "فعال", "Active") : UiText.T(Context, "غیرفعال", "Inactive"))
                                </span>
                            </td>
                            <td class="ak-col-actions">
                                <div class="dropdown ak-row-menu">
                                    <button type="button" class="ak-row-menu-btn" data-bs-toggle="dropdown" aria-expanded="false" aria-label="@UiText.T(Context, "عملیات", "Actions")">
                                        <i class="bi bi-three-dots-vertical" aria-hidden="true"></i>
                                    </button>
                                    <ul class="dropdown-menu dropdown-menu-end">
                                        <li><a class="dropdown-item" asp-action="Details" asp-route-id="@item.Id"><i class="bi bi-eye" aria-hidden="true"></i>@UiText.T(Context, "جزئیات", "Details")</a></li>
                                        <li><a class="dropdown-item" asp-action="Edit" asp-route-id="@item.Id"><i class="bi bi-pencil" aria-hidden="true"></i>@UiText.T(Context, "ویرایش", "Edit")</a></li>
                                        <li><hr class="dropdown-divider" /></li>
                                        <li><partial name="~/Views/Shared/Partials/_ToggleActiveButton.cshtml" model="@(("Sarrafs", item.Id, item.IsActive, "dropdown-item ak-danger", (string?)returnUrl))" /></li>
                                    </ul>
                                </div>
                            </td>
                        </tr>
                    }
                    @if (!Model.Items.Any())
                    {
                        <tr>
                            <td colspan="8">
                                <div class="ak-empty">
                                    <i class="bi bi-shop" aria-hidden="true"></i>
                                    @UiText.T(Context, "صرافی یافت نشد", "No sarrafs found")
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
